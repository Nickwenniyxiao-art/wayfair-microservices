apiVersion: apps/v1
kind: Deployment
metadata:
  name: wayfair-product-service
  namespace: wayfair
  labels:
    app: wayfair-product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wayfair-product-service
  template:
    metadata:
      labels:
        app: wayfair-product-service
    spec:
      containers:
      - name: product-service
        image: us-central1-docker.pkg.dev/cohesive-poetry-486213-q3/wayfair/wayfair-product-service:latest
        ports:
        - containerPort: 3001
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3001"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: wayfair-product-service
  namespace: wayfair
  labels:
    app: wayfair-product-service
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
  selector:
    app: wayfair-product-service
