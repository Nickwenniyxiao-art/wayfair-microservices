apiVersion: apps/v1
kind: Deployment
metadata:
  name: wayfair-shipping-service
  namespace: wayfair
  labels:
    app: wayfair-shipping-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wayfair-shipping-service
  template:
    metadata:
      labels:
        app: wayfair-shipping-service
    spec:
      containers:
      - name: shipping-service
        image: us-central1-docker.pkg.dev/cohesive-poetry-486213-q3/wayfair/wayfair-shipping-service:latest
        ports:
        - containerPort: 3006
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3006"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: wayfair-shipping-service
  namespace: wayfair
  labels:
    app: wayfair-shipping-service
spec:
  type: ClusterIP
  ports:
  - port: 3006
    targetPort: 3006
    protocol: TCP
  selector:
    app: wayfair-shipping-service
