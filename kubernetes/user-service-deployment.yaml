apiVersion: apps/v1
kind: Deployment
metadata:
  name: wayfair-user-service
  namespace: wayfair
  labels:
    app: wayfair-user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wayfair-user-service
  template:
    metadata:
      labels:
        app: wayfair-user-service
    spec:
      containers:
      - name: user-service
        image: us-central1-docker.pkg.dev/cohesive-poetry-486213-q3/wayfair/wayfair-user-service:$SHORT_SHA
        ports:
        - containerPort: 3002
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3002"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: wayfair-user-service
  namespace: wayfair
  labels:
    app: wayfair-user-service
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
  selector:
    app: wayfair-user-service
